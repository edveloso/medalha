<%= simple_form_for(@usuario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :setor %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
